__pycache__/
*.pyc
.venv/
.pytest_cache/

# Secrets. Never commit this file.
.env

# Per-stage debug snapshots, regenerated on every run.
# NOTE: a trailing comment on the same line as a pattern does NOT work in
# .gitignore — "data/raw/  # snapshots" is read as a literal path and matches
# nothing. That bug tracked 4.1 MB of snapshots per run.
data/raw/
